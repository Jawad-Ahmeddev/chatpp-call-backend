module.exports = function(req, res, next) {
    // Placeholder middleware for future JWT authentication
    // Right now, it just passes the request through to the next middleware/route
    next();
};
